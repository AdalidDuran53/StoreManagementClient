/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { ActionResult } from '../../models/action-result';

export interface VersionClientsLoginPost$Plain$Params {
  version: string;
  userName: string;
  password: string;
}

export function versionClientsLoginPost$Plain(http: HttpClient, rootUrl: string, params: VersionClientsLoginPost$Plain$Params, context?: HttpContext): Observable<StrictHttpResponse<ActionResult>> {
  const rb = new RequestBuilder(rootUrl, versionClientsLoginPost$Plain.PATH, 'post');
  if (params) {
    rb.path('version', params.version, {});
    rb.query('userName', params.userName, {});
    rb.query('password', params.password, {});
  }

  return http.request(
    rb.build({ responseType: 'text', accept: 'text/plain', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<ActionResult>;
    })
  );
}

versionClientsLoginPost$Plain.PATH = '/{version}/Clients/Login';
