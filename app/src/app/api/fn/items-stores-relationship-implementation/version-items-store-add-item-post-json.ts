/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { ActionResult } from '../../models/action-result';

export interface VersionItemsStoreAddItemPost$Json$Params {
  version: string;
  clientId: string;
  sessionId: string;
  itemID: string;
  storeId: string;
  operationDate: string;
}

export function versionItemsStoreAddItemPost$Json(http: HttpClient, rootUrl: string, params: VersionItemsStoreAddItemPost$Json$Params, context?: HttpContext): Observable<StrictHttpResponse<ActionResult>> {
  const rb = new RequestBuilder(rootUrl, versionItemsStoreAddItemPost$Json.PATH, 'post');
  if (params) {
    rb.path('version', params.version, {});
    rb.query('clientId', params.clientId, {});
    rb.query('sessionId', params.sessionId, {});
    rb.query('itemID', params.itemID, {});
    rb.query('storeId', params.storeId, {});
    rb.query('operationDate', params.operationDate, {});
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'text/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<ActionResult>;
    })
  );
}

versionItemsStoreAddItemPost$Json.PATH = '/{version}/ItemsStore/AddItem';
